<Window x:Class="Diachkov_Case_Demo.View.Windows.MenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diachkov_Case_Demo.View.Windows"
        mc:Ignorable="d"
        Title="MenuWindow" Height="1080" Width="1920" MinHeight="1000" MinWidth="1900" WindowStartupLocation="CenterScreen">
    <Grid Background="#3c3c3c">
        <!--Разметка для всего окна-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="0.1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="50"/>
        <ListBox x:Name="Lst" Grid.Column="1" Grid.Row="3" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!--Разметка для ListBox-->
                    <Grid Background="#3c3c3c" Height="550" Width="450">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="0.1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="3*"/>
                            <RowDefinition Height="2*"/>
                            <RowDefinition Height="3*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <!--Элементы для ListBox-->
                        <Image Grid.Column="1" Source="{Binding Photo}" Height="200" Width="200"/>
                        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="1">
                            <TextBlock Foreground="White" Text="{Binding Name}" FontSize="36" TextWrapping="Wrap"/>
                            <TextBlock Foreground="White" Text="{Binding Category.Title}" FontSize="14"/>
                        </StackPanel>

                        <TextBlock Foreground="White" Grid.Column="1" Grid.Row="2" Text="{Binding Descrition}" TextWrapping="Wrap" FontSize="14"/>

                        <TextBlock Foreground="White" Grid.Column="1" Grid.Row="3" Text="{Binding Price}" FontSize="24"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>